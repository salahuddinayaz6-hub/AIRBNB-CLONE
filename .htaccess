# Airbnb Clone .htaccess
DirectoryIndex index.php
 
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
 
    # Redirect to index.php if file/dir not found
    # RewriteCond %{REQUEST_FILENAME} !-f
    # RewriteCond %{REQUEST_FILENAME} !-d
    # RewriteRule . index.php [L]
</IfModule>
 
